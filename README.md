# Portfolio Template

A simple portfolio template that consists of a hero, services, tech stack, experience, and contact.
The data shown can easily be replaced by modifying the data.json; just make sure that only the values are modified and not the property names and structure of the data. 

This project was created with HTML, Vanilla JS, CSS, and Boxicons.
